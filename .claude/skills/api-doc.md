# 生成 API 接口文档

扫描 `routes/` 目录下所有路由文件，生成一份接口文档：

## 输出格式

对每个接口输出：
- **URL**：`GET /api/notices`
- **说明**：通知列表
- **请求参数**：query 参数和含义
- **需要登录**：是/否
- **需要角色**：teacher/leader 等
- **返回示例**：JSON 格式

注意前端是鸿蒙开发，用的是DevEco。
最后输出一个完整的 Markdown 文档，可直接发给前端同事。
